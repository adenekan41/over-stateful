import { createContext } from 'react';

/**
 * Decalare overstateful context
 * We need to decalare overstateful context to be consumed by overstateful core itself
 * @function
 * @constant
 * @default
 */

const OverContext = createContext(null);

export default OverContext;
